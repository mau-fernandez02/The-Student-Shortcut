---

---

<section class="sec-4">
  <div class="layout-videos-D-C-E">
    <div class="box-video-1">
      <video autoplay loop muted playsinline class="video-holder">
        <source src="/media/#" type="video/mp4" />
        Tu navegador no soporta la reproducción de video.
      </video>
    </div>
    <div class="box-video-2">
      <video autoplay loop muted playsinline class="video-holder">
        <source src="/media/#" type="video/mp4" />
        Tu navegador no soporta la reproducción de video.
      </video>
    </div>
    <div class="box-video-1">
      <video autoplay loop muted playsinline class="video-holder">
        <source src="/media/#" type="video/mp4" />
        Tu navegador no soporta la reproducción de video.
      </video>
    </div>
  </div>
  <div class="sec-4-text">
    <h1>¿POR QUÉ IRME DE INTERCAMBIO?</h1>
    <p>
      Intentar empaparse los beneficios de un intercambio se queda corto frente a lo que realmente se experimenta.
    </p>
    <p>
      Durante tu estadía, expandirás tus horizontes mentales y forjarás una versión mucho más madura y segura de ti mismo. Académicamente, te sumergirás en nuevas metodologías de estudio mientras te conviertes en hablante fluido de un nuevo idioma.
    </p>
    <p>
      Pero el verdadero cambio sucede en otro nivel: la mayoría de los estudiantes descubre su auténtica vocación, empoderando al regresar a país a construir su futuro.
    </p>
    <button>Quiero comenzar mi intercambio</button>
  </div>
</section>

<style>
  .sec-4 {
    max-width: min(85%, 990px);
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 40px;
    padding: 60px 0px;
  }

  .layout-videos-D-C-E {
    display: grid;
    /* Proporción correcta: 115px : 450px : 115px = 1fr : 3.913fr : 1fr */
    grid-template-columns: 115px 448px 115px;
    grid-template-rows: 448px;
    gap: 20px;
    
  }

  /* Videos laterales: 115px x 450px = aspect-ratio 115/450 */
  .box-video-1 {
    width: 100%;
    aspect-ratio: 115 / 450;
    background-color: black;
    overflow: hidden;
    border-radius: 20px;
  }

  /* Video central: 450px x 450px = aspect-ratio 1/1 */
  .box-video-2 {
    width: 100%;
    aspect-ratio: 1 / 1;
    background-color: black;
    overflow: hidden;
    border-radius: 20px;
  }

  .video-holder {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .sec-4-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 20px;
    max-width: 1200px;
    width: 100%;
  }

  .sec-4-text button {
    color: white;
  }
  .sec-4-text h1 {
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    color: white;
  }

  .sec-4-text p {
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    line-height: 1.6;
    color: white;
  }

  /* Responsivo para pantallas medianas */
  @media (max-width: 990px) {
    .layout-videos-D-C-E {
      grid-template-columns: 1fr;
      overflow: hidden;
      gap: 20px;
      max-width: 100%;
    }
    

    .box-video-1 {
      width: 0%;
      height: 0%;
      
    }

    .box-video-2 {
      aspect-ratio: 1 / 1;
    }
  }

  /* Responsivo para pantallas pequeñas */
  @media (max-width: 480px) {
    .layout-videos-D-C-E {
      grid-template-columns: 1fr;
      gap: 12px;
      max-width: 100%;
    }

    .box-video-1:nth-child(1),
    .box-video-1:nth-child(3) {
      display: none;
    }

    .box-video-2 {
      aspect-ratio: 1 / 1;
      max-width: 280px;
    }
  }
</style>